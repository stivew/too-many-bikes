<a href={{@route}} class="card-content">
    <img class="card-image" src={{@imagePath}} alt={{@imageAltText}}>
    <p>
        {{#if @displayText}}
            {{@displayText}}
        {{else}}
            {{@route}}
        {{/if}}
    </p>
</a>